# Themes

Themes supply CSS, block patterns, and an optional demo home. They do not
replace the core EJS layout.

See [docs/THEMES.md](../docs/THEMES.md) for resolution order and file layout.
The bundled example is `themes/default`.
