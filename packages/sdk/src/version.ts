// SPDX-License-Identifier: MIT

/** Version of the published `@justflows/sdk` package. */
export const SDK_VERSION = "0.1.5";

/** Major revision of the runtime contract exposed through `PluginContext`. */
export const SDK_API_VERSION = 1;
